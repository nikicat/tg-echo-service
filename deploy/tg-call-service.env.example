# Copy to ~/.config/tg-call-service/tg-call-service.env and fill in.
# Telegram API credentials — create an app at https://my.telegram.org/apps
API_ID=
API_HASH=

# Optional: seconds to delay before echoing the caller's audio back.
#ECHO_DELAY=2
